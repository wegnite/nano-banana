{
  "scripts": {
    "build:cloudflare": "next build && npx @cloudflare/next-on-pages",
    "preview:cloudflare": "npm run build:cloudflare && wrangler pages dev .vercel/output/static",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler pages deploy .vercel/output/static"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.8.0",
    "wrangler": "^3.22.0"
  }
}